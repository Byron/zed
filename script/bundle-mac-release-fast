#!/usr/bin/env bash

set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export ZED_BUNDLE_BUILD_FLAG="--profile release-fast"
export ZED_BUNDLE_TARGET_DIR="release-fast"

exec "${script_dir}/bundle-mac" "$@"
